---
import { Sprite } from "astro-icon";
interface Props {
  as?: keyof astroHTML.JSX.IntrinsicElements;
  className?: string;
  size?: "sm" | "xs";
}

const { as: Wrapper = "button", size, className, ...rest } = Astro.props;
---

<Wrapper {...rest} class:list={["icon-btn", size, className]}>
  <Sprite name="fi_heart" />
</Wrapper>
