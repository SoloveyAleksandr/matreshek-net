---
interface Props {
  as?: keyof astroHTML.JSX.IntrinsicElements;
  className?: string;
}

const { as: Wrapper = "button", className, ...rest } = Astro.props;
---

<Wrapper {...rest} class:list={["default-btn", className]}>
  <slot />
</Wrapper>
