---
interface Props {
  as?: keyof astroHTML.JSX.IntrinsicElements;
  className?: string;
  size?: "sm" | "xs";
  color?: "light";
}

const { as: Wrapper = "button", className, size, color, ...rest } = Astro.props;
---

<Wrapper {...rest} class:list={["default-btn", size, color, className]}>
  <slot />
</Wrapper>
